
==== BROADCAST =============================================

<%
	broadcasts.each { broadcast ->
		println broadcast.text
	}
%>
Running GVM server ${gvmVersion} on vertx ${vertxVersion}

============================================================
